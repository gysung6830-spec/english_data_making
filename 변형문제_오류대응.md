# 변형문제 — 실제 출제상 오류 가능성과 대응방안

오류를 두 종류로 나눈다.

- **A. 구조·조판 오류** — 프로그램이 깨지거나 문항 형식이 어긋남. **현재 대부분 방어됨**(재시도/보정/에러).
- **B. 내용 타당성 오류** — 문항 형식은 멀쩡한데 **정답이 둘이거나, 정답이 틀리거나, 모호**함.
  **현재는 프롬프트 지시에 의존**하며 자동 검증(verifier의 내용검증)은 **미구현**. → 가장 큰 리스크.

---

## 1) 구조·조판 오류 (현재 방어 상태)

| 오류 | 현재 대응 |
|------|-----------|
| 순서 덩어리 수·given_n 오산 | `make_order` 자동 재분배/클램프 |
| 삽입 위치 번호 범위 초과 | `make_insert` 내부문장 클램프 |
| 밑줄 단어 대소문자/철자 불일치 | 단어경계→대소문자무시→부분일치 폴백 |
| 밑줄 중첩(같은 단어 두 번) | 플레이스홀더 치환으로 중첩 차단, 중복 지정 시 실패→재시도 |
| G 일치 개수 0개/선지 불일치 | 1~5개 강제, 선지 동적 생성 |
| 빈칸(F) 어구 다중 출현 | 지문 내 유일성 검사→실패 시 재시도 |
| 약어(Dr./U.S.)에서 문장 오분리 | 약어 보호 후 분리 |
| 머리글/제목 특수문자로 PDF 깨짐 | 조판 이스케이프 |
| 유형 생성 실패 | 유형별 2회 재시도 후 명확한 에러 |

→ 이 층위는 "LLM이 이상하게 답해도 프로그램이 안 깨진다"까지 확보됨.

---

## 2) 내용 타당성 오류 (유형별) — 리스크와 대응

> 공통 리스크: **정답=유의어**로 바꾸는데 오답도 우연히 맞거나(복수정답), 유의어가 문맥상 부정확(정답오류).

### ① 순서 배열
- **오류**: 단서(연결사·지시어)가 약해 **두 순서 다 자연스러움** → 정답 모호.
- **대응**: 블록 경계를 '지시어 시작 지점'으로 잡도록 프롬프트 강화(현재 폴백은 균등분할). 검증: 정답 순서만 지시관계가 성립하는지 자기검증.

### ② 문장 삽입
- **오류**: 빼낸 문장이 **여러 위치에 들어가도 말이 됨**(지시어 없는 문장 선택 시).
- **대응**: 연결사·지시어/소재연결 문장만 선택(프롬프트). 검증: 정답 위치의 지시관계가 유일한지.

### ③ 주제 / ⑥ 내용일치 / 2회 B·G
- **오류**: 오답(무관·모순·미언급)이 사실 **부분적으로 맞아 정답급**이 되거나, 정답 유의어가 **과대일반화**.
- **대응**: 각 선지에 "지문 어느 문장과 어떻게 일치/불일치"를 근거로 달게 하고(일부 구현), 그 근거로 **자기검증(복수정답·오독 체크)**.

### ④ 어휘
- **(1) 반의어형 오류**: 나머지 4개를 유의어로 바꿨는데 **문맥상 어색**해지면 오답이 2개.
- **(2) 부정어형 오류**: 부정어를 넣었는데 문장이 **여전히 말이 됨**(이중부정 등).
- **대응**: 유의어 교체 후 문맥 유지 여부, 부정 삽입 문장의 확실한 모순 여부를 자기검증.

### ⑤ 어법 / 2회 A·C
- **오류**: '정상'으로 둔 밑줄이 사실 애매하게 틀리거나, '틀린' 밑줄이 **허용되는 용법**. 복수정답 개수 어긋남.
- **대응(가장 확실)**: 어법은 **규칙기반 탐지**(품사태깅 spaCy/nltk)로 전환 — 명세 §5.1의 원안. 현재는 LLM이 오답 형태를 주므로 문법 오판 가능.

### ⑦ 서술형(2) / 2회 D — 어순 배열
- **오류**: 낱개 단어가 **정답 외 다른 문법적으로 맞는 문장**으로도 배열됨 → 유일 복원 실패.
- **대응**: "보기 단어로 정답 문장이 **유일하게** 복원되는지" 검증(파서/규칙). 문장을 짧고 어순이 하나로 고정되게 선택.

### ⑦ 서술형(3) / 2회 E — 요약·빈칸
- **오류**: 제시어 어형이 **둘 다 가능**(과거/현재완료 등)하거나, E에서 **오답 한 쌍이 우연히 양쪽 맞음** → 복수정답.
- **대응**: 어형을 하나로 확정하는 문맥 신호를 넣고, E는 각 오답의 **틀린 칸을 명시**해 자기검증.

### 2회 F — 빈칸추론
- **오류**: 빈칸이 **주제가 아닌 곳**을 뚫거나, 정답 유의어가 부정확, 오답이 사실 성립.
- **대응**: "핵심(주제) 문장 한 곳"으로 위치 고정 + 유일 등장(구현). 정답 유일성·오답 불성립 자기검증.

---

## 3) 우선순위 대응방안 (권장)

1. **자기검증(LLM self-check) 단계 추가** — 생성 후 별도 호출로
   ① 정답이 유일한가 ② 각 오답이 정말 틀린가 ③ 정답 근거가 지문에 있는가
   를 판정 → 실패 시 해당 유형 재생성. (명세 §6 verifier의 내용검증 구현)
2. **정답/오답 근거 필드 강제** — 모든 정답에 '지문 근거 문장 인용', 오답에 '어느 부분과 모순/무관'을
   필수화(일부 구현) → 자기검증의 입력으로 사용.
3. **어법·어순의 규칙기반화** — 품사태깅(spaCy/nltk)으로 비문 판정·유일 복원 검사를 결정적으로.
   (LLM 문법 오판 리스크 제거)
4. **난이도별 오답 개수 대조** — "심은 오답 수 = 정답 개수" 자동 대조(복수·개수형).

> 현 상태: 1~4는 **아직 미구현**(프롬프트 지시로만 유도). 구조 오류는 방어됨.
> 실제 API로 세트를 돌려 위 항목을 표본 점검하면 어느 유형이 취약한지 바로 드러난다.
