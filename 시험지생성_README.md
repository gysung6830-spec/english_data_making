# 영어 시험지 자동 생성 툴

영어 지문을 넣으면 지문마다 **수능형 변형문제 6종 + 서술형 3소문항**을 자동 생성해,
**고등 모의고사 스타일 2단 PDF**로 내려주는 도구입니다.
(같은 저장소의 *지문 분석 도구*(`README.md`)와는 별개의 프로그램입니다.)

> 설계 근거: 저장소의 코딩 명세서(v1.1). 미결정 없음(설계 확정).

---

## 무엇을 만드나 — 고정 7종

어떤 지문이든 **항상 아래 7종이 이 순서로** 나옵니다(유형 누락·순서 변경 불가).

| 순서 | 유형 | 핵심 |
|------|------|------|
| ① | 순서 배열 | (A)(B)(C) 순서 섞기, 보기 5개 |
| ② | 문장 삽입 | 연결사·지시어로 위치가 확정되는 문장만 빼서 ①~⑤ 위치 표시 |
| ③ | 주제(영어 선지) | 정답=핵심어 **유의어**로 변형 / 무관 2·모순 2, 오답에 지문 단어 섞어 함정 |
| ④ | 어휘 | 두 방식 선택: **(1) 반의어형** 정답=반의어·나머지=유의어(형용사·부사·동사 위주) / **(2) 부정어 삽입형** 선지는 원문 그대로이되 정답 문장에 no/not/neither 삽입 → 문맥 모순 |
| ⑤ | 어법(복수정답) | 최대 8밑줄 중 **여러 개**를 어법상 틀리게 → "모두 고르시오" |
| ⑥ | 내용 일치 | **한글 선지** 5개, 1개만 일치. `hard` 모드는 뻔한 정반대 대신 오답 4개를 **부분일치+한요소왜곡 / 주체 바꿔치기 / 인과날조·역전 / 미언급인데 그럴듯** 4기법으로 하나씩 만들고, 정답은 유의어로 위장. 해설에서 각 오답의 **틀린 부분+함정 유형**을 짚음 |
| ⑦ | 서술형 3소문항 | (1)이해확인=한글답 (2)동사원형 낱개배열 영작 (3)요약문 (A)(B) 어형변화 빈칸 |

**출력 배치:** 여러 지문이면 **[모든 지문의 문제 → 그다음 모든 지문의 해설]**.
문항 번호는 문서 전체에서 **연속**(지문2는 7번부터). 문제와 해설은 같은 번호로 대응.

---

## 웹앱으로 실행 (브라우저에서 바로)

```bash
bash setup.sh          # 최초 1회 (라이브러리 + 한글 폰트)
python webapp.py       # → http://127.0.0.1:5000
```

- **두 가지 문제 세트를 체크박스로 선택** — 원하는 것만 각각 별도 PDF로 나옵니다.
  - **변형문제 1회**: 어법·어휘·주제·내용일치·순서·삽입·서술형 (7종)
  - **변형문제 2회**: 어법·어휘짝짓기(A)·함의추론(B)·어법판별(C)·어순배열(D)·요약빈칸(E)·빈칸추론(F)·내용일치개수(G)
  - 둘 다 체크하면 `(지문명)_변형문제_1회.pdf` / `(지문명)_변형문제_2회.pdf` 두 개가 생성됩니다.
- **입력 방법 두 가지** — ① 지문 붙여넣기(여러 개면 `---` 한 줄로 구분) 또는
  ② **파일 업로드(PDF·사진 JPG/PNG/WEBP, 여러 개 = 여러 지문)**.
  - 글자 PDF는 그대로 텍스트를 읽고, 사진/캡처/스캔은 **Claude 비전이 지문을 읽어** 처리합니다.
  - PDF는 **한글 해석·머리글을 자동 제거**하고 영어 지문만 추립니다. 한 PDF에 지문이
    여러 개면(예: EBS 워크시트) **지문별로 자동 분리**되어 각각 문항이 만들어집니다.
  - 넣은 지문은 **그대로** 문제에 쓰입니다(분석 AI가 지문을 바꿔 말해도 원문으로만 출제).
- 머리글·어휘 방식·내용일치 난이도를 고른 뒤 **‘시험지 생성 →’** 을 누르면 브라우저에서
  바로 PDF를 미리보기/다운로드합니다.
- **API 키가 있어도 자동으로 쓰이지 않습니다.** 파란 **‘시험지 생성’** 버튼을 누를 때만 API가
  호출됩니다(비용 발생). 비용 없이 연습·미리보기만 하려면 회색 **‘무료 미리보기’** 버튼을 누르세요
  (내장 예시 지문으로 디자인만 확인, API 미사용).
- **미리보기 전용 모드**: 키가 .env에 있어도 아예 쓰고 싶지 않을 땐
  `python webapp.py --preview` (또는 `EXAM_PREVIEW_ONLY=1`)로 실행하면 키를 무시하고
  무료 미리보기만 됩니다.
- 옵션: `python webapp.py --host 0.0.0.0 --port 8080`

## 빠른 시작 — 파일/CLI로 미리보기(웹앱 없이)

```bash
bash setup.sh                      # 최초 1회 (라이브러리 + 한글 폰트)
python 시험지생성_데모코드.py       # 데모 2지문(DNA·star manager) → output/데모_시험지.pdf
```

- `--header "○○학원 고3 영어"` 로 상단 머리글 문구를 넣을 수 있습니다(기본값 비움).
- 데모는 **검증 → 2단 PDF 조판/출력** 파이프라인을 API 없이 확인시켜 줍니다.

## 실제 사용 — 내 지문으로 생성

```bash
# 1) .env 에 ANTHROPIC_API_KEY 입력
# 2) input/ 폴더에 지문 파일을 넣기 — .txt / .pdf / .jpg / .png (파일 1개 = 지문 1개, 여러 개 가능)
python make_exam.py --header "○○학원 고3 영어"     # → output/시험지.pdf
```

옵션: `--out 경로.pdf`, `--input 폴더`, `--demo`(API 없이 데모),
`--vocab-method {synonym,negation}`(어휘 방식: 반의어형 / 부정어 삽입형, 기본 synonym),
`--content-difficulty {plain,hard}`(내용일치 오답 난이도, 기본 hard=헷갈리는 함정 오답).

> 데모 PDF 에서는 지문 1 어휘를 **반의어형**, 지문 2 어휘를 **부정어 삽입형**으로 보여
> 두 방식을 한눈에 비교할 수 있습니다.

---

## 내부 구조 (부품 = 모듈)

각 부품은 한 가지 일만 하고, **새 유형은 생성기 파일 하나만 추가**하면 확장됩니다.
모든 생성기는 같은 방식(분석 결과를 받아 → 문제/해설 HTML 을 돌려줌)으로 동작하므로,
본체는 유형을 몰라도 `TYPE_ORDER` 대로 실행만 합니다.

```
exam/
  types.py        핵심 데이터 구조 (TYPE_ORDER 고정, Source·Passage{q,a})
  analyzer.py     분석기 — 지문 1회 분석(문장·핵심어+유의어/반의어·주제·문법밀집문장)
  build.py        정본 변형기 — 하나의 지문(문장 리스트)에서 6종을 파생(단일 지문 공유)
  format.py       문제/해설 HTML 빌더 (볼드 5곳 규칙의 단일 출처)
  schemas.py      각 단계 LLM 응답의 구조화 JSON 스키마(pydantic 검증)
  llm.py          AI 연결기 (기존 src.client.ClaudeClient 재사용)
  generators/
    order.py insert.py topic.py vocab.py grammar.py content.py short_answer.py
  validator.py    검증기 — 6종+해설 완비·유형집합 일치·번호 연속
  renderer.py     조판기 — 2단·지문라벨·연속번호·[문제 전체→해설 전체]
시험지생성_데모코드.py   데모 진입점(부록 산출물): 지문데이터→검증→2단 PDF
make_exam.py             실제 진입점(CLI): input/*.txt → Claude API → PDF
webapp.py                웹앱 진입점(Flask): 브라우저에서 붙여넣기→생성→미리보기/다운로드
web/                     Flask 앱(app.py) + templates/ + static/
```

### 단일 지문 공유(중요)

6종은 모두 **하나의 정본 지문(문장 리스트)** 에서 파생됩니다(명세서 §3·§5).
생성기는 지문을 다시 쓰지 않고 **"어떻게 변형할지"(문장 번호·단어·순서)** 만 정하며,
실제 조립은 `build.py` 가 정본 문장에서 수행합니다. 그래서 유형별로 지문이 재서술되지
않고, 연결 텍스트는 모두 동일하며 **각 유형의 편집 부분만** 달라집니다.

| 유형 | 정본에 가하는 변형 |
|------|------|
| 주제 | 변형 없음(원본 그대로) |
| 순서 | 문장을 (A)(B)(C) 덩어리로 쪼개 **순서만** 섞음 |
| 삽입 | 문장 **하나만** 빼내고 위치 ①~⑤ 표시 |
| 어휘 | **지정 단어 5개만** 유의어/반의어 치환(또는 정답 문장에 부정어) |
| 어법 | **지정 단어 최대 8개만** 오답형으로 |
| 내용 일치 | 변형 없음(원본 그대로) + 한글 선지 |
| 서술형 | 원본 그대로 + 파생 과제(영작·요약) |

**동작 순서(명세서 §5)**

```
지문 입력 → [분석] 1회 → [생성] 6종 고정 순서 → [검증] → [조판] → [출력]
```

---

## 볼드 규칙 (구분용, 5곳만 굵게)

1. 제목 "영어 영역" + 상단 배지  2. 문항 번호  3. 지문 라벨 `[지문 1]`
4. 정답·해설 헤더 + 각 문항 **정답 글자**  5. 서술형 `<보기>` 제목 + **괄호 안 제시어**

그 외(지문 본문·선택지·해설 설명글·발문)는 모두 얇게.

---

## 개발자용: 오프라인 테스트

```bash
python tests/test_exam_offline.py
```

데모 검증·번호 연속·볼드 규칙·조판, 그리고 **가짜 클라이언트로 LLM 경로 배선**까지
API 없이 검증합니다.
