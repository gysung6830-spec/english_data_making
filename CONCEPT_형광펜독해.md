# 기획서 — 「형광펜 독해」

> **한 줄 컨셉: 수능 영어, 무엇을 읽고 무엇을 버릴지 '칠하는 법'을 기출로 배우는 교재.**

3~5등급이 시간에 쫓기는 진짜 이유는 실력이 아니라 **지문을 다 읽어서**다.
1등급은 안 읽는 곳을 안다. 이 교재는 그 **"칠하는 기술"** 하나를 기출로 훈련시킨다.
분석·해설이 아니라 **읽는 눈을 바꾸는 훈련서**다.

---

## 1. 확정 사항

| 항목 | 결정 |
|---|---|
| 과목/범위 | 수능 영어 (독해 유형 중심) |
| 타깃 | 3~5등급 → 2등급 (시간부족·실수로 점수 새는 층) |
| 핵심 스킬 | **선택적 읽기 = 형광펜 칠하는 법** (신호로 무조건/스킵 판별) |
| 성격 | 답지 ❌ / **훈련서** ⭕ — 학생이 스스로 칠하게 만든다 |
| 형태 | 기존 자동생성 도구의 새 출력물 (`outputs.highlight`) |
| 소재 | 실제 기출 지문 (2022 6월 모평 등) |
| **다루는 유형** | **근거 응집형만** (대의·함축·빈칸·순서·삽입·요약·장문 제목/지칭). 세부 대조형(일치·도표·안내문·어법·어휘)은 **제외** |

### 왜 "칠하는 법"이 컨셉인가
- 형광펜을 **미리 칠해 주면** 학생은 못 배운다 → 새 지문 앞에서 또 다 읽는다.
- 가르쳐야 할 것은 결과(어디가 노랑)가 아니라 **판별 근거(왜 노랑)** = 정답을 몰라도 보이는 **신호**.
- 그래서 이 교재의 심장은 **「신호 사전」** 하나. 이걸 체화하면 어떤 지문이든 스스로 칠한다.

---

## 1-1. 교재 목차(구성)

| 파트 | 내용 | 산출물 |
|---|---|---|
| **PART 0. 엔진** | 신호 사전(§2) + "노랑만 읽기" 적용범위(§3-2) | 앞부분 참조 페이지 |
| **PART 1. 유형별 훈련** | 번호대별(§4) — **펼침면 구성**: 왼쪽=문제 페이지(직접 풀기·워크시트), 오른쪽=STEP2 훈련+STEP3 해석 | `strategy_compact_sample.html` |
| **PART 2. 패러프레이징 특별 훈련** ★별도 | ① 방법론(원리·5변환·함정 4유형) ② **평가원 지문 내 패러프레이징 해부**(실제 기출로 지문어→정답어 단어 매핑) ③ 객관식 10문항 + 정답·해설 | `paraphrase_section.html` |

> 패러프레이징은 **모든 유형을 관통하는 스킬**이라 유형 훈련에서 분리해 **독립 섹션**으로 집중 훈련한다.
> 메인(PART 1)에서는 캐치법 블록을 빼고, 문항별 적용(한 줄 패러프레이즈 캐치)만 남겼다.

### 디자인 톤
- **기본 톤 = 딥그린**(`#1f7a5c`). 강조는 **노랑(형광펜)·빨강(함정/오답)**만 튀게, 나머지는 딥그린/회색으로 정돈.
- 해부 파트: 지문 핵심어=노랑 하이라이트 → 정답 표현=연녹 하이라이트, 변환유형 배지(동의어·구체→추상·품사·반대구조·비유→직설).

---

## 2. 엔진 — 「신호 사전」 (확장판, 한 번 외우면 모든 지문에 적용)

**🟡 무조건 읽기 신호**

| 분류 | 신호어 |
|---|---|
| 역접·대조 | However·But·Yet·Nevertheless·In contrast·On the other hand·Instead / **Unlike, not A but B, rather than** |
| 결론·인과 | Thus·Therefore·So·As a result·Consequently / **because·since·lead to·result in** (인과=논리 뼈대) |
| 강조·주장 | should·must·need to / important·key·essential·crucial / **the most·the only·the single**(최상·유일) / In fact·Indeed·Above all / It is X that (강조구문) |
| 한정·조건·예외 | only·only when·if·unless·except·as long as → **정답이 숨는 단골** |
| 통념·반전 | "Many believe / It is thought"(통념) → **바로 뒤 역접이 진짜 주제** / Surprisingly·Contrary to·Paradoxically |
| 정의·재정의 | is defined as·means·refers to / **콜론( : ) 뒤** |
| 태도·평가어(±) | 긍정 benefit·valuable ↔ 부정 problem·risk·illusion·fail → 대의·함축·어휘 정답 방향 |
| 위치 | 첫 문장 · 각 단락 첫 문장 · 마지막 문장 · 빈칸/밑줄 문장 |
| **지시·연결(대용어)** | this·these·that·those·such(a)·one·another·its·the former/latter → **순서·삽입(35–38)의 핵심** — 앞 문장을 가리켜 흐름을 잇는다 |
| **첨가·병렬 / 비교** | not only~but also·moreover·in addition·as well as·besides / similarly·likewise·just as → 같은 방향 강화·논지 확장 |

> **데이터 기반 보강**: 학습된 220개 지문(2022-06~2027-06) 빈도 분석으로 실제 자주 나오는 신호를 추가.
> 특히 지시·연결어(that 550·this 170·one 100·these 56·such·another…)가 최다 → 순서·삽입 카테고리 신설.
> 역접(rather·still·by contrast·no longer)·강조(especially·particularly·notably)·인과(thereby·in order to)·예시스킵(take·consider·imagine·including)도 보강. `src/ingest_bank.py`의 신호점수 계산에도 반영(재계산 완료).

**⬜ 스킵 신호**: For example·For instance·such as·like·to illustrate(예시) / 숫자·연도·고유명사·인명 나열 / 재진술 반복 / 양보절(Although·Despite~ — 주절이 핵심) / 긴 관계절 수식

> 원칙: **사전은 풍부하게, 카드는 간결하게.** 각 문항 카드에는 그 지문에서 실제로 울린 신호 2~3개만 표시한다.
> 이 표는 교재 맨 앞 1페이지. 모든 문항 카드가 이 신호를 재사용한다.

---

## 2-1. 두 번째 엔진 — 「패러프레이즈 캐치법」 (정답 찍는 핵심)

**원리: 정답은 뜻은 같고 단어를 바꾼다. 오답은 단어를 그대로 두고 뜻을 왜곡한다.**
평가원 정답은 지문의 paraphrase, 오답은 지문 단어의 표면 복사. 그래서 "빨리 캐치 = 빨리 정답".

**3초 캐치 절차**
1. 형광펜 문장의 **핵심어에 ○**
2. 선지 스캔 — **지문 단어를 그대로 복사한 선지 = 함정, 먼저 소거** / **뜻이 다른 단어로 바뀐 선지 = 정답 후보**
3. 남은 '바꿔 말한' 선지가 정답

**평가원 단골 변환 패턴 (사전화)**

| 패턴 | 지문 → 정답 선지 |
|---|---|
| 동의어 치환 | regulate/set → control·determine·govern |
| 구체→추상(상위어) | lion·songwriter·engineer → creatures·creative people |
| 품사 전환 | decide(동) → decision·decision-making(명) |
| 반대구조 | does not shrink → expands / only at boundary → associate location |
| 비유→직설 | an empty inbox → satisfying others' demands |

> 카드마다 **「패러프레이즈 캐치」 줄** 1개: `지문어 ⟷ 정답어`(교체) 표시 + 오답의 '그대로 복사'를 빨강 표시.
> 검증(2022 6월): 34번 `boundary/territory`→`locality`(동의·추상), 오답 `complexity·recorded` 복사 / 21번 `empty inbox`(비유)→`others' demands`(직설), 오답 `social` 복사.

---

## 3. 문항 1개 = 학습 루프 4단계 (이게 훈련서의 핵심)

각 기출 문항을 이 흐름으로 훈련시킨다. **글은 최소, 한 문항 = 반쪽.**

1. **직접 칠해보기** — 신호 태그 없는 '민' 지문 제시. 학생이 신호 사전 보고 스스로 형광펜.
2. **정답 칠 공개 (왜 노랑)** — 🟡 표시 + 각 칠에 신호 태그(역접/한정/결론…). "정답 몰라도 이 신호로 여기다."
3. **정답·오답 찍기** — 칠한 근거 문장 `⟶` 정답. 오답은 ✘ 한 줄: **표면어휘 재사용 함정** vs **논리 반대**.
4. **공식 한 줄** — 이 유형 통용 규칙. (예: "빈칸 = 역접·한정 문장 찾아 추상화")

### 3-1. 카드는 2종 분리 — 「훈련 카드」 + 「해석 카드」
- **훈련 카드**: 위 1~4단계. 영어·신호·찍기 중심, **한글 없음**(깔끔하게 유지).
- **해석 카드**(별도): 2단 표(왼쪽 영어 / 오른쪽 한글). **🟡 형광펜 문장 + 정답·오답 선지만** 해석.
  스킵(회색) 문장은 해석하지 않는다 — 안 읽어도 되는 곳이므로. 해석 부담까지 줄여 "선택적 읽기"를 강화.

---

## 3-1-2. 왜 노란색만 읽어도 답이 나오나 (원리)

기계적 공식이 아니라 글의 구조에서 나오는 자연스러운 결과다.

- 수능 지문은 예외 없이 **하나의 주장**을, **예시·부연으로 늘려 쓴 글**이다.
  🟡 노란색 = 주장이 드러나는 자리(주제문·방향 트는 역접·결론·빈칸) / ⬜ 회색 = 그 주장을 설명하는 예시·부연.
- **예시(살)를 빼도 주장(뼈대)은 그대로다** — 예시는 주장을 '설명'할 뿐 '바꾸지' 못한다.
- **평가원 정답은 언제나 뼈대(주장)에서** 나온다 — 예시 속 낱낱의 사실이 아니라, 글이 향하는 논지에서.
- 그래서 **노란 문장만 이으면 글의 논지가 그대로 재구성**되고, 정답 근거가 손에 들어온다. 회색은 시간만 먹는다.

> 구조: `🟡 주제 → ⬜ 예시 → ⬜ 부연 → 🟡 역접·결론(재진술)`. 카드의 "🟡만 읽어보면" 줄이 이 원리를 지문마다 문장으로 보여준다.

---

## 3-2. 다루는 유형 = "노랑만 읽기가 통하는 유형"만 (범위 확정)

이 교재는 **근거 응집형 유형만** 다룬다. "칠하는 법 + 노랑만 읽기 + 패러프레이즈"가 통하는 유형으로 **범위를 좁혀 방법론을 완전히 일관**시킨다.

**✅ 포함 (근거 응집형)** — 답 근거가 주제·역접·결론 문장에 모임 → 노랑만으로 풀림
> 목적 · 심경 · 주장 · 요지 · 주제 · 제목 · **함축 · 빈칸** · 무관한 문장 · 순서 · 삽입 · 요약 · 장문(제목·지칭)

**❌ 제외 (세부 대조형)** — 정독·역스캔이 필요해 이 방법론과 안 맞음 → **교재에서 삭제**
> ~~내용일치~~ · ~~도표~~ · ~~안내문~~ · ~~어법~~ · ~~어휘(문맥)~~ · ~~장문 어휘·내용일치~~

- **안전장치**: (포함 유형 내에서) 노랑만으로 선지가 안 갈리면 → 회색으로 한 단계 확장(예시가 근거인 드문 경우). 회색은 "안 읽는다"가 아니라 "정독하지 않는다".
- 카드마다 **「🟡만 읽기 검증」 줄** 1개: 노란 문장만 이어 정답까지 도달하는지 실제로 보여준다(2022 6월 34·21번 검증 완료).

---

## 4. 번호대별 — '칠하는 법'이 번호대마다 다르다

수능 영어 45칸 중 **근거 응집형만** 다룬다(세부 대조형 삭제). 번호대마다 **어떤 신호를 어디에 칠하고, 정답이 어디 숨는지**가 다르므로 각각 1벌씩.

| 번호대 | 다루는 유형 | 이 번호대의 '칠하는 법' 포인트 |
|---|---|---|
| 18–20 | 목적·심경·주장 | 첫·끝 문장 + 주장 신호(should)만. 예시 통째 스킵 |
| **21·22–24** | 함축·요지·주제·제목 | 밑줄 ±판정 → 역접 뒤 주제문 칠, 예시 스킵 |
| **31–34** | 빈칸 *(킬러)* | 역접·한정(However·only·If) 문장 칠 → 추상화. 패러프레이즈 캐치 |
| 35–37 | 무관한 문장·순서 | 지시어·연결어(this·however·연대) 칠로 흐름 잇기 |
| 38–40 | 삽입·요약 | 논리 공백 지점 칠 / 요지 압축 |
| 41·43·44 | 장문(제목·지칭) | 첫 문단 주제 고정 + 지칭 추적, 나머지 속독 |

> **제외**: 25 도표 · 26 내용일치 · 27–28 안내문 · 29 어법 · 30 어휘 · 42 장문어휘 · 45 장문일치.
> 우선순위: **31–34(빈칸) → 21·22–24** 부터. (배점·킬러·"칠하는 법" 학습효과 최대)

---

## 5. 기출로 검증됨 (2022 6월 모평)

- **34번 빈칸**: `However`(역접)+`only when`(한정) 겹친 문장이 정답 근거 → **한정 신호 = 빈칸 정답 단골** 규칙 실증.
- **21번 함축**: `But ~ abandon your dreams for` 역접이 밑줄 ±를 못박음 → 정답 ②(추상화), 오답은 `social`·`innovative` 표면어휘 재사용.
- 샘플: `samples/strategy_compact_sample.html` (카드 v2, 신호 태그 포함)

---

## 5-1. 학습형 지문 은행 (넣는 PDF마다 축적·교체)

넣는 기출 PDF를 **모두 저장하고 학습**해, 유형별로 가장 적합한 지문을 교재가 쓰게 한다.

- **적재기**: `src/ingest_bank.py` — PDF → 문항 분해 → 번호대/유형 분류(근거 응집형만) → 지문·선지 추출 → **신호 점수** 계산 → `corpus/passage_bank.jsonl` 누적
- **신호 점수(fitness)** = 역접·한정·결론·주장·인과·통념 신호어 개수 × 길이 적합도. **형광펜 학습에 적합할수록 높음.**
- **자동 교체**: 같은 유형 슬롯에서 **더 높은 점수의 지문이 들어오면 대표 지문으로 승격**. → PDF를 넣을수록 각 유형 대표 지문이 더 좋은 것으로 갱신된다.
- **영구 보존**: `passage_bank.jsonl`은 git 커밋(세션 초기화돼도 유지). 원본 PDF는 `corpus/pdfs/`에 보관(저작권·용량으로 git 제외).
- 상세: `corpus/README.md`
- 실측(2022 6월 적재): 16개 지문 학습, 빈칸 대표=#33(신호점수 6.0, 역접 5), 함축 대표=#21(2.4).
- **구문해설 교재와 데이터 공유**: 형광펜 교재가 입력 창구. `ingest_bank`가 원본을 `corpus/pdfs/`에 보관하고 은행에 적재하며, 새 시험지는 구문해설 처리 큐(`input/`)에도 자동 공유 → 한 번만 올리면 양쪽이 같은 지문 사용. 구문해설은 성공 처리한 PDF를 `input/_done/`으로 옮겨 재분석·재과금 방지.
- **정답표 자동 연결**: 정답표 PDF를 넣으면 `exam_id`로 시험을 매칭해 **공식 정답·배점을 은행 문항에 연결**(순서 무관). → 카드 정답을 추론이 아니라 공식 정답으로 채움. (실측: 2023-06 정답표 45개 → 은행 16문항 연결, 21번 함축 정답 ⑤ 등)
- **현황(2022-06 + 2023-06 학습)**: 32개 지문, 정답 연결 16개. 유형별 대표는 신호점수로 자동 선정(빈칸 대표 6.0 등).

```bash
python -m src.ingest_bank 시험지.pdf     # 학습(누적)
python -m src.ingest_bank --report        # 은행 현황
python -m src.ingest_bank --pick 31-34    # 유형별 대표 지문
```

---

## 6. 기존 시스템과의 결합 (구현 메모)

- `config.yaml` `outputs`에 `highlight: true/false` + 웹앱 체크박스
- 파일명: `(지문명)_형광펜독해.pdf`
- 파이프라인: 본문+문제·선지·번호 유지 → 문장 분해 → 문장별 `{signal, must_read/skip}` 라벨 JSON
- 렌더: 문장별 라벨을 배경색(🟡/⬜)으로, 신호는 인라인 태그로. '민 지문(1단계)'과 '정답 칠(2단계)' 2뷰 생성
- 정답/오답: `{정답번호, 근거문장, 오답별 함정유형(표면어휘/논리반대/무관)}` JSON
- 디자인: 시그니처 틸색 + 형광펜 2색(연노랑/연초록)

---

## 7. 다음 단계

1. 「신호 사전」 확정 + 번호대별 신호 가중치 미세조정 (유형마다 중요 신호가 다름)
2. 31–34(빈칸) 카드 완성본 → 학습 루프 4단계(직접칠→정답칠→찍기→공식) 반영
3. `src`에 `highlight` 생성기(프롬프트+문장 라벨 JSON 스키마)
4. `templates`에 카드 HTML/CSS (민지문/정답칠 2뷰)
5. `--mock` 프리뷰 → 21–24 등 다른 번호대로 확장
