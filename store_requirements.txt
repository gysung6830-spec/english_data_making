# 판매 사이트(store.py)만 돌리는 데 필요한 라이브러리입니다.
# 자료를 만드는 내부 도구(requirements.txt)와 달리 아주 가볍습니다.
flask>=3.0
gunicorn>=21.0

# PDF 에 구매자 표시(워터마크)를 새기는 데 씁니다.
# 없어도 사이트는 돌아갑니다 — 표시 없이 원본이 그대로 나갑니다.
pypdf>=5.0
reportlab>=4.0
fonttools>=4.50
openpyxl>=3.1  # 단어장 엑셀 읽기

# 자료를 '화면에서 보고 인쇄' 하려면 PDF 를 쪽마다 그림으로 바꿔야 합니다.
# 단어 시험지 미리보기도 이걸 씁니다. 없으면 파일 받기로만 돌아갑니다.
PyMuPDF>=1.24
