# 🌿 Ortica영어 판매 사이트 사용 설명서

`store.py` 는 **고객이 보는 자료 판매 사이트**입니다.
자료를 만드는 내부 도구(`webapp.py`)와 **완전히 분리**되어 있어서,
판매 사이트를 인터넷에 올려도 API 키나 내부 도구는 노출되지 않습니다.

```
webapp.py   ← 나만 쓰는 자료 제작 도구 (API 키 들어감, 비밀번호로 잠금)
store.py    ← 고객이 보는 판매 사이트   (API 키 없음, 누구나 접속)
```

---

## 1. 내 컴퓨터에서 열어보기

```bash
pip install -r store_requirements.txt
python store.py
```

그다음 브라우저 주소창에 **http://localhost:5001** 을 입력하세요.
종료는 실행한 터미널에서 `Ctrl + C`.

> 관리자 페이지(주문 확인)까지 보려면 아래처럼 비밀번호를 정해서 실행합니다.
> ```bash
> ADMIN_PASSWORD=내가정한비밀번호 python store.py
> ```
> 그리고 **http://localhost:5001/admin** 으로 접속합니다.

---

## 2. 가게 정보 바꾸기 — `store_data/site.json`

메모장으로 열어서 따옴표 **안쪽 글자만** 바꾸면 됩니다.
(따옴표나 쉼표를 지우면 사이트가 안 열립니다. 지우지 마세요.)

| 항목 | 어디에 보이나 |
|---|---|
| `contact.email` / `contact.phone` | 화면 맨 아래, 이용 안내, 주문 완료 화면 |
| `contact.kakao_url` | 상단 '문의하기' 버튼 (비워 두면 버튼이 안 보임) |
| `contact.hours` | 상담 가능 시간 안내 |
| `payment.bank_name` / `bank_account` / `bank_holder` | **주문 완료 화면의 입금 계좌** |
| `payment.kakaopay_url` | 카카오페이 송금 링크 (비워 두면 버튼이 안 보임) |
| `payment.notice` | 발송까지 걸리는 시간 안내 문구 |
| `business.*` | 화면 맨 아래 사업자 정보 (전자상거래법상 표기 항목) |
| `policy.refund` / `license` / `privacy` | 이용 안내 페이지의 환불·이용범위·개인정보 |
| `subscription_teaser` | 홈 화면의 '월 구독 준비 중' 안내. `enabled` 를 `false` 로 하면 사라집니다 |

바꾼 뒤에는 **브라우저 새로고침만** 하면 바로 반영됩니다. (서버 재시작 불필요)

---

## 3. 상품 추가·수정·삭제 — `store_data/products.json`

지금 들어 있는 5개는 **예시**입니다. 실제 자료에 맞게 고쳐 주세요.

상품 하나는 이렇게 생겼습니다.

```json
{
  "slug": "mock-2026-09",              ← 주소에 쓰이는 영문 이름 (겹치면 안 됨)
  "category": "mock",                  ← 아래 categories 의 id 중 하나
  "name": "2026학년도 9월 모의평가 전지문 세트",
  "subtitle": "18~45번 지문 전체",
  "price": 22000,                      ← 실제 판매가 (숫자만, 원 단위)
  "list_price": 30000,                 ← 정가. 적으면 할인율이 자동 표시됩니다 (없어도 됨)
  "passages": 28,
  "grade": "고3",
  "badge": "신규",                      ← 카드 위 빨간 라벨 (없어도 됨)
  "sort": 30,                          ← 목록 정렬 순서 (숫자가 작을수록 위)
  "active": true,                      ← false 로 바꾸면 사이트에서 숨겨집니다
  "includes": ["지문 분석지", "핵심 어휘 리스트", "영단어 시험지"],
  "description": "상세 페이지 첫 문단에 나오는 설명",
  "highlights": ["체크 표시로 나오는 장점", "여러 줄 가능"],
  "delivery": "입금 확인 후 영업일 기준 24시간 이내 이메일 발송",
  "format": "PDF (A4, 인쇄용)",
  "sample_file": "sample_mock.pdf"     ← 무료 샘플 파일 이름 (없어도 됨)
}
```

- **상품 추가** — 위 덩어리를 통째로 복사해 `products` 목록에 붙이고, 앞 상품 뒤에 쉼표(`,`)를 찍습니다.
- **잠깐 안 팔기** — `"active": false` 로 바꿉니다. (지우지 않아도 됨)
- **분류 추가** — `categories` 에 `{ "id": "workbook", "name": "부교재" }` 처럼 추가합니다.

> ⚠️ 저장한 뒤 사이트가 안 열리면 쉼표·따옴표가 하나 빠진 것입니다.
> [jsonlint.com](https://jsonlint.com) 에 붙여 넣으면 어디가 틀렸는지 알려 줍니다.

---

## 4. 무료 샘플 PDF 넣기

**샘플은 매출에 가장 크게 영향을 줍니다.** 사기 전에 품질을 눈으로 봐야 지갑이 열립니다.

1. 내부 도구로 만든 PDF 중 잘 나온 것을 하나 고릅니다.
2. `store_data/samples/` 폴더에 넣습니다.
3. `products.json` 의 `sample_file` 에 **그 파일 이름 그대로** 적습니다.

파일이 없으면 '샘플 준비 중'으로 표시되고 버튼이 눌리지 않습니다.

---

## 5. 주문이 들어오면 — 관리자 페이지 `/admin`

주문서가 제출되면

1. 주문이 **DB에 저장**되고,
2. (메일 설정을 했다면) **내 메일로 알림**이 오고,
3. 고객에게는 **입금 계좌 화면**이 보입니다.

`/admin` 에서 할 일은 이렇습니다.

| 상태 | 언제 바꾸나 |
|---|---|
| `입금대기` | 주문 접수 직후 (자동) |
| `입금확인` | 통장에서 입금을 확인했을 때 |
| `발송완료` | 고객 이메일로 자료를 보낸 뒤 |
| `취소` | 입금이 안 되거나 고객이 취소했을 때 |

- 위쪽 숫자 카드에서 **입금 대기 금액 / 입금 확인 누적**을 한눈에 볼 수 있습니다.
- **CSV로 백업** 버튼으로 전체 주문을 엑셀 파일로 내려받을 수 있습니다. 가끔 받아 두세요.

---

## 6. 인터넷에 올리기 (Render 무료)

1. [render.com](https://render.com) 가입 → **New → Blueprint** → 이 저장소 선택
2. `render.yaml` 을 자동으로 읽어 서비스 2개를 만듭니다.
   판매 사이트만 필요하면 `ortica-store` 만 남기고 다른 하나는 지워도 됩니다.
3. 대시보드에서 아래 값을 입력합니다.

| 이름 | 값 |
|---|---|
| `ADMIN_PASSWORD` | 주문 관리 페이지 비밀번호 (**꼭 정하세요**) |
| `BANK_ACCOUNT` | 입금 계좌번호 (site.json 에 직접 적어도 됨) |
| `ORDER_EMAIL_TO` | 주문 알림을 받을 내 메일 주소 |
| `SMTP_HOST` `SMTP_USER` `SMTP_PASS` | 아래 7번 참고 |

4. 몇 분 뒤 `https://ortica-store.onrender.com` 같은 주소가 생깁니다.

> **무료 요금제 주의**
> - 한동안 접속이 없으면 잠들어서 **첫 접속이 30초쯤 느립니다.**
> - **재배포하면 저장 공간이 초기화**되어 주문 DB가 지워질 수 있습니다.
>   → 그래서 아래 **주문 알림 메일**을 꼭 설정하세요. 메일함이 백업이 됩니다.
>   → 주문이 꾸준히 들어오기 시작하면 유료 플랜(월 $7)으로 올리는 걸 권합니다.

---

## 7. 주문 알림 메일 설정 (지메일 기준)

1. 구글 계정 → 보안 → **2단계 인증**을 켭니다.
2. **앱 비밀번호**를 만듭니다. (16자리가 나옵니다)
3. Render 대시보드에 이렇게 넣습니다.

```
SMTP_HOST = smtp.gmail.com
SMTP_PORT = 587
SMTP_USER = 내주소@gmail.com
SMTP_PASS = 앱비밀번호16자리
ORDER_EMAIL_TO = 알림받을주소@gmail.com
```

설정하지 않아도 사이트는 정상 작동합니다. 다만 알림 메일만 안 옵니다.

---

## 8. 문 열기 전 체크리스트

- [ ] `site.json` 의 **이메일 · 전화 · 입금 계좌**를 실제 값으로 바꿨다
- [ ] `site.json` 의 **사업자 정보**(상호·대표·사업자등록번호·통신판매업신고번호·주소)를 채웠다
      — 온라인으로 물건을 팔면 전자상거래법상 표기 의무가 있습니다.
      사업자등록과 통신판매업 신고는 [정부24](https://www.gov.kr)에서 할 수 있습니다.
- [ ] `products.json` 의 **예시 상품 5개를 실제 자료로** 교체했다
- [ ] `store_data/samples/` 에 **무료 샘플 PDF**를 최소 1개 넣었다
- [ ] `ADMIN_PASSWORD` 를 정했다 (안 정하면 `/admin` 이 아예 잠깁니다)
- [ ] 주문서를 **직접 한 번 넣어 보고** 알림 메일이 오는지 확인했다

---

## 9. 나중에 카드 결제를 붙이려면

지금 구조는 **결제만 빼고 다 만들어져 있습니다.** 나중에 토스페이먼츠를 붙일 때는
`store.py` 의 `order()` 함수에서 주문을 저장한 직후에 결제 요청을 넣고,
결제 성공 콜백에서 상태를 `입금확인` 으로 바꾸면 됩니다.
상품·주문서·관리자 페이지는 그대로 씁니다.

토스페이먼츠는 **사업자등록증 + 통신판매업 신고**가 있어야 계약할 수 있습니다.

---

## 10. 자주 겪는 문제

- **사이트가 안 열려요 / 500 오류** → `site.json` 이나 `products.json` 에서 쉼표·따옴표가 빠졌을 가능성이 큽니다.
- **`/admin` 이 "비밀번호가 설정되어 있지 않습니다" 라고 나와요** → `ADMIN_PASSWORD` 환경변수를 정하고 다시 실행하세요.
- **주문 알림 메일이 안 와요** → 사이트는 정상입니다. `SMTP_*` 값을 확인하세요. 지메일은 일반 비밀번호가 아니라 **앱 비밀번호**여야 합니다.
- **주문이 사라졌어요** → 무료 요금제에서 재배포한 경우입니다. 알림 메일 설정 + 주기적인 CSV 백업으로 대비하세요.

---

## 개발자용: 테스트

```bash
python -m tests.test_store
```

API 키·인터넷 없이 고객 페이지, 주문서 검증, 관리자 기능, 보안을 확인합니다.
