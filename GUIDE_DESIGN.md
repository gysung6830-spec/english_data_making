# 구문해석 실전서 — 설계 문서

평가원 기출로 익히는 구문해석 실전서. 5개년 평가원 자료를 학습해, **추상적이고 수식어가 많은
평가원 문장을 오역 없이 뚫는 법**을 담은 실전 지침서.

기존 `지문 자동 분석 도구`(src/, 지문 1개 → 6섹션 분석지)와는 **목적·산출물이 다른 별도 파이프라인**이다.
→ 새 모듈 `src/guide/` 로 분리 구현. 기존 도구는 그대로 둔다.

---

## 1. 교재 전체 구조 (0~2부)

### 표지
`평가원 기출로 익히는 / 구문해석 실전서 / 단어를 몰라도, 문장이 복잡해도 핵심을 놓치지 않는 법`

### 0부 — 기본기: 단어 몰라도 복잡한 문장 뚫는 법  *(고정 콘텐츠, 기출 데이터 불필요)*
| 방법 | 내용 |
|---|---|
| 방법① 괄호치기 | 수식어(분사구문·관계사절)를 통째로 `( )` 로 묶어 눈에서 지운다 |
| 방법② 뼈대 찾기 | 괄호를 건너뛰고 진짜 주어+동사(S/V)부터 해석 |
| 방법③ 연결어로 합치기 | 괄호 속을 `~하면서 / ~해서 / ~한 채로 / ~라면` 중 문맥에 맞게 붙인다 |

→ 이 파트는 방법 설명 + 예문 몇 개(하드코딩/템플릿). API 불필요.

### 1부 — 평가원 기능어 특집: **"답으로 이어지는 평가원 코드"**  ★ 이번 핵심
평가원 코드 VOCA(단어장)의 카테고리를 그대로 챕터로 삼는다.

| Ch | 카테고리(VOCA DAY) | 대표 오역 유형 |
|---|---|---|
| Ch1 | 인과 (DAY19) | 화살표 방향 뒤집힘 (원인↔결과) |
| Ch2 | 대조·반의 (DAY17) | 대조 신호 놓쳐 같은 방향으로 읽음 |
| Ch3 | 등호·구별 (DAY18) | A=B(동격) 를 놓치거나, 구별을 동일시 |
| Ch4 | 비교·대체·선호 (DAY16) | 우열·선호 대상이 뒤바뀜 |
| Ch5 | 연결어 (DAY20) | 양보/인과/예시 논리 흐름 오독 |

각 챕터 = `카테고리 도입(방향표/신호 정리)` + **코드별 카드** N개.

**코드 카드 1장 구조** (요청하신 형식):
```
[기출 문장]  ── 코드 어구를 하이라이트
  A. (오역)  흔한 잘못된 해석 + 왜 틀렸는지 (방향/논리 근거)
  B. (정답)  올바른 해석 방향
  💡 핵심 팁  이 코드를 만나면 무엇부터 확인할지
```
→ 샘플의 `Ch1. 인과표현` 페이지가 이 카드의 프로토타입.

### 2부 — 구문해석(문법구조): 평가원 빈출 구문
| Ch | 구문 |
|---|---|
| Ch1 | 분사구문 (괄호치기 실전) |
| Ch2 | 관계사절 (수식어 건너뛰기) |
| Ch3 | 삽입·동격 |
| Ch4 | 도치·강조 |
| Ch5 | 가정법·비교구문 |

각 = 기출 예문 + 뼈대/괄호 표시 + 합친 해석 + **셀프체크**(학생이 직접 괄호치기).

---

## 2. 생성기 아키텍처

```
src/guide/
  codes.yaml      평가원 코드 VOCA 데이터 (카테고리 → 코드 어구 목록 + 뜻 + 방향 메타)
  corpus.py       사용자가 넣은 기출 PDF/이미지 → 문장 분리 → 각 코드 어구가 든 문장 매칭
  prompts.py      코드 카드 생성 프롬프트 (오역 A / 정답 B / 팁)  + 구문 카드 프롬프트
  schemas.py      카드/챕터 JSON 스키마 (pydantic 검증)
  build.py        코드별 매칭 문장 → Claude 카드 생성 → 검증(+재시도)
  render.py       실전서 PDF 조립 (표지 + 0/1/2부)
  templates/      guide 전용 HTML/CSS (샘플 디자인 재현)
run_guide.py      진입점:  python run_guide.py  /  --mock
samples/guide_mock.py   API·코퍼스 없이 볼 수 있는 목 데이터
```

### 데이터 흐름
```
평가원 코드 VOCA (codes.yaml) ─┐
                              ├→ [코드별] 기출 문장 매칭
사용자 기출 PDF/이미지 ────────┘        ↓
                              Claude: 오역 A / 정답 B / 팁 생성 (JSON)
                                       ↓  스키마 검증(+실패 시 재요청)
                              실전서 PDF (표지·0·1·2부)
```

- **예문 조달**: 기출 우선. 사용자가 `input_corpus/` 에 5개년 기출 PDF/캡처를 넣으면
  `corpus.py` 가 문장을 분리하고, 각 코드 어구(정규식/구문 매칭)가 등장하는 문장을 모은다.
  해당 코드에 기출 문장이 없으면 → (선택) Claude가 평가원 스타일 예문 생성으로 보완.
- **비용/안정성**: 기존 도구와 동일 원칙 — 카드마다 구조화 JSON 강제, 개수·필드 검증, 실패 1회 재시도,
  일부 실패해도 전체 진행. Batch API 옵션.
- **미리보기**: `python run_guide.py --mock` → 코퍼스·API 키 없이 샘플 PDF 생성(디자인 확인용).

### codes.yaml 예시
```yaml
categories:
  - id: causation
    title: "인과"
    day: 19
    misread: "화살표 방향 뒤집힘 (원인↔결과)"
    direction:
      forward:  ["lead to", "result in", "cause", "trigger", "bring about"]   # A→B, A가 원인
      backward: ["result from", "stem from", "be attributable to", "arise from", "be due to"]  # B←A, A가 결과
  - id: contrast
    title: "대조·반의"
    day: 17
    ...
```

---

## 3. 구현 순서 (제안)
1. **1부 '평가원 코드' 카드 생성기 먼저** (요청 핵심) — codes.yaml(5개 카테고리) + corpus 매칭 + 카드 프롬프트/스키마 + guide 템플릿 + 목 렌더러 → **샘플 PDF** 확인
2. 확정 후 **0부(기본기)·2부(구문)** 챕터 추가
3. 웹앱/배치 연동, 나머지 VOCA DAY(전 38일) 코드 확장

> 5개년 기출 코퍼스와 API 키는 실제 생성 시 사용자가 제공. 지금 단계는 **1번의 코드+샘플 PDF**까지.
