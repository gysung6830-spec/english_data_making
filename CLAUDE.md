# 프로젝트 규칙 (영어 지문 학습자료 생성기 · passage3)

이 저장소의 작업 시 **항상** 지킬 규칙.

## 모의고사 처리
- **모의고사인 경우에만 27·28번을 제외한다.** 모의고사 27·28번은 실용문
  (안내문·광고)이라 지문 학습자료로 부적합. **교재 등 다른 자료는 제외하지 않는다.**
  `run(drop_practical=True)`가 기본값이며 webapp의 '27·28번 제외' 체크박스도 기본 체크.
  (CLI에서 굳이 포함하려면 `--keep-2728`.)
- **모의고사 판별**은 `is_mock_exam()`로 한다: 파일명/제목/원문에 모의고사 키워드
  (모의고사·학력평가·수능·교육청·평가원 등)가 있거나, 장문 범위 라벨(41~42번·43~45번)이
  있으면 모의고사로 본다. 판별되지 않으면 27·28번을 그대로 둔다.
- 제외는 renumber 이후에 적용되어 실제 문항번호(…26, 29, 30…)를 유지한다.

## 직독직해(끊어읽기) 어투
- **자연스러운 직독직해**: 영어 어순은 유지하되 번역투(억지 `~것이다/~라면이다/
  있어서다`, 비술어에 `다` 부착) 금지. 구/절 조각은 자연스러운 조사·어미로 끝냄.
- 조각↔뜻 1:1 대응: 각 조각의 뜻은 그 조각 단어만 번역(다음 조각 것 당겨오기 금지).
- that·관계사·종속접속사는 **그 단어에서 새 조각 시작**(앞 조각 끝에 매달지 않음).

## 출력물 공통
- 저작권 표기: 좌하단 `©2026.Ortica영어.All rights reserved`, 우하단 페이지번호(11px).
- 모든 UI/출력은 한국어. 폰트는 NanumSquareRound(base64 임베드).
- JSON 재입력 시 재분석(API) 생략 → 비용 0. (번역·어휘·문장분리·청크 모두 있으면 건너뜀)

## 기타
- 컨테이너는 임시본. git이 원본. 작업 후 지정 브랜치에 커밋·푸시.
- 테스트: `python passage3/tests/test_parser_render.py`
