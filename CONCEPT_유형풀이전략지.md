# 기획서 — 「유형 풀이 전략지」 (4번째 출력물)

> 기존 도구는 **지문을 깊게 분석**한다(직독직해·문법·어휘).
> 이 교재는 **문제를 빠르게 푸는 법**을 가르친다 — 지문 내용이 아니라 *유형별 풀이 알고리즘 + 시간 전략*이 주인공.

한 줄 컨셉: **"기출을 다 풀지 마라 — 유형별 풀이 공식으로 최소 문항 최대 점수."**

---

## 1. 확정 컨셉 (합의 완료)

| 항목 | 결정 |
|---|---|
| 과목/범위 | **수능 영어 영역만** (독해 유형 중심) |
| 타깃 등급 | **3~5등급 → 2등급** (실수·시간부족을 잡아 점수를 크게 올리는 층) |
| 형태 | 기존 자동생성 도구의 **4번째 출력물** (`outputs.strategy`) |
| 문제·선지 처리 | **있으면 살려서 활용** (실전 풀이·함정선지 분석) / 없으면 유형 자동 추론 |

### 핵심 뒤집기
지금 도구는 지문 하나씩 처리하며 전처리에서 문제·정답을 제거하고 본문만 남긴다.
"풀이법 교재"는 원래 *문제*를 푸는 방법이라 성격이 안 맞는데, 이걸 뒤집어 무기로 만든다:

> **"이 지문엔 어떤 유형이 붙는지 판별 → 그 유형 풀이 공식을 이 지문에 실전 적용."**

유형 이론을 따로 외우는 게 아니라, **지문마다 그 지문에 맞는 공식이 딸려 나온다.**
3~5등급 학생이 "이런 지문 보면 → 이 순서로 → 이렇게 답 찾는다"를 실제 기출 위에서 체화한다.

---

## 2. 출력물 구성 (6섹션)

| # | 섹션 | 내용 | 3~5등급 훅 |
|---|---|---|---|
| 1 | **유형 판별** | 이 지문에 붙는 대표 유형(빈칸/순서/삽입/주제·제목/어법/어휘/함축·지칭 등) + 배점·난이도 | "뭘 묻는 문제인지"부터 |
| 2 | **풀이 알고리즘** | 그 유형의 3~5스텝 공식 (예: 빈칸 = ① 빈칸 앞뒤 연결어 ② 주제문 찾기 ③ 선지 소거) | 외울 순서가 명확 |
| 3 | **이 지문 실전 적용** | 위 스텝을 이 지문에 하나씩 밟아 정답까지 도달 | 공식이 진짜 통하는 걸 눈으로 확인 |
| 4 | **함정·오답 소거** | 이 유형이 잘 파는 함정 패턴 + 매력적 오답 걸러내기 (문제·선지 있으면 실제 선지로) | 실수 감점 방지 |
| 5 | **시간 전략** | 이 유형 목표 시간 + "막히면 버려라/찍어라" 판단 기준 | 시간부족 해결 |
| 6 | **한 줄 요약 공식카드** | 이 유형 핵심을 카드 한 장으로 압축 (모아서 회독·오답노트化) | 회독용 압축 |

---

## 3. 유형 분류(taxonomy) 초안 — 수능 영어 독해

- 대의파악: 목적 · 심경/분위기 · 주장 · 요지 · 주제 · 제목
- 세부/실용: 내용일치·불일치 · 도표 · 실용문(안내문)
- 어법·어휘: 어법(밑줄) · 어휘(문맥상 낱말)
- 빈칸추론(단어/구/절) — *킬러*
- 간접쓰기: 무관한 문장 · 글의 순서 · 문장 삽입 · 요약문
- 장문: 지칭추론 · 함축의미 · 장문 독해

각 유형별로 §2의 "풀이 알고리즘 / 함정 패턴 / 목표 시간" 템플릿을 1벌씩 보유.

---

## 4. 기존 시스템과의 결합 (구현 메모)

- `config.yaml`의 `outputs`에 `strategy: true/false` 키 추가 → 웹앱 체크박스 1개 추가
- 파일명 규칙: `(지문명)_유형풀이전략지.pdf`
- 처리 파이프라인: 본문 추출 후, 문제·선지 잔존 여부를 감지하는 스텝 추가
  - 문제 있음 → 실제 유형·정답·선지로 §3·§4 채움
  - 문제 없음 → 지문 성격으로 "붙을 만한 유형" 추론해 §1~§6 채움
- 출력은 기존처럼 **구조화 JSON → HTML → PDF**, 섹션 개수 검증 재사용
- 디자인: 기존 6색 스킴 위에 풀이 전용 시그니처 컬러 1색(예: 남색/틸)로 통일

---

## 5. 다음 단계

1. 유형별 풀이 알고리즘·함정·목표시간 템플릿 콘텐츠 작성(유형 taxonomy 1벌)
2. `src`에 `strategy` 섹션 생성기(프롬프트+JSON 스키마) 추가
3. `templates`에 전략지 HTML/CSS 추가
4. `config.yaml`·웹앱에 토글 노출
5. `--mock` 샘플 데이터로 디자인 프리뷰
