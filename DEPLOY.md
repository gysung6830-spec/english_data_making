# 📱 아이패드·폰에서 쓰기 (인터넷에 올리기)

아이패드에는 파이썬을 깔 수 없어서, 웹앱을 **인터넷 서버에 한 번 올려두면**
아이패드·폰·다른 컴퓨터 어디서든 **주소(링크)만 열면** 쓸 수 있습니다.

여기서는 가장 쉬운 **Render.com**(무료 시작 가능)에 올리는 방법을 안내합니다.
필요한 파일(`Dockerfile`, `render.yaml`)은 이미 저장소에 들어 있어요.

---

## 준비물
1. **GitHub 계정** — 이 코드가 올라가 있는 곳 (이미 있음)
2. **Anthropic API 키** + 크레딧 충전 (console.anthropic.com)
3. **정할 것 2가지**: 웹앱에 접속할 때 쓸 **비밀번호** 하나

---

## 1단계 — Render 가입
1. 주소창에 **render.com** 입력 → 접속
2. **Get Started / Sign Up** → **GitHub 계정으로 로그인** (제일 간편)

## 2단계 — 이 코드 연결해서 배포
1. Render 화면 오른쪽 위 **New +** → **Blueprint** 클릭
2. 이 저장소(**english_data_making**)를 선택 → **Connect**
3. 배포할 **브랜치**를 코드가 있는 브랜치로 지정
   (`claude/english-passage-analysis-tool-giy07l`, 또는 main에 합쳤다면 main)
4. Render가 `render.yaml`을 자동으로 읽어 설정을 잡아줍니다 → **Apply**

## 3단계 — 비밀정보 입력 (중요)
배포 중에 아래 두 칸을 입력하라고 나옵니다:
- **ANTHROPIC_API_KEY** → 본인 API 키(`sk-ant-...`) 붙여넣기
- **APP_PASSWORD** → 웹앱 접속용 비밀번호 직접 정해서 입력
  (이 비밀번호를 아는 사람만 웹앱을 쓸 수 있어요)

> `APP_SECRET`은 자동 생성되니 건드리지 않아도 됩니다.

## 4단계 — 완료 후 주소 확인
- 빌드가 끝나면(5~10분) `https://english-passage-analyzer.onrender.com` 같은 **주소**가 생겨요.
- 이 주소를 **아이패드 사파리**에서 열기 → 비밀번호 입력 → 사용!
- 사파리에서 **공유 → 홈 화면에 추가**를 하면 앱처럼 아이콘으로 열 수 있어요. 📲

---

## 알아두면 좋은 점
- **무료 요금제**는 일정 시간(약 15분) 안 쓰면 잠자기 상태가 돼요. 이때 첫 접속은
  깨어나느라 **50초쯤 느릴 수 있어요**(그다음부턴 빠름). 항상 켜두려면 유료(월 $7)로 올리면 됩니다.
- 웹앱을 쓸 때마다 **본인 API 키로 요금이 나갑니다**(지문당 대략 100~200원). 비밀번호로 잠가 두었으니
  본인·공유한 사람만 쓸 수 있어요. 비밀번호는 가끔 바꿔주면 더 안전합니다.
- API 키/비밀번호는 코드가 아니라 **Render 설정(환경변수)**에만 저장돼서 안전합니다.

---

## 문제가 생기면
- **빌드 실패** → Render의 **Logs** 화면을 캡처해서 보여주세요.
- **주소를 열었는데 로그인 화면만 나옴** → 정상입니다. 3단계에서 정한 비밀번호를 넣으세요.
- **비밀번호 화면조차 안 나옴** → 아직 빌드 중일 수 있어요. 몇 분 뒤 새로고침.

막히면 **어느 단계에서 무엇이 보이는지**만 알려주세요. 거기서부터 같이 풀어드릴게요.
